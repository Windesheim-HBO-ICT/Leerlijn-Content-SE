---
title: 2. Oefeningen Enum
taxonomie: ['ib-19.2.Enums.DT', 'ib-19.3.Enums.DT']
tags:


---

> Meer informatie over [[1. Uitleg Enum|enum]]

## Opdracht 1
Maak een variabele van het type `enum` aan en print de waarde.

### Specificaties
- Er is een `enum` genaamd `Kleur` met de waarden `Rood`, `Groen` en `Blauw`.
- Een variabele `MijnKleur` krijgt de waarde `Kleur.Rood`.
- De waarde van `MijnKleur` wordt geprint in de console.

### Verwachte output
Als `MijnKleur == Kleur.Rood`, komt in de console:
```
Rood
```

### Nu jij
``` csharp runner
using System;

enum Kleur {}

Kleur MijnKleur =
Console.WriteLine(MijnKleur); // Print Rood
``` 

> [!info]- Mogelijke uitwerking
> ``` csharp
> using System;
> enum Kleur 
> {
>     Rood,
>     Groen,
>     Blauw
> }
> 
Kleur MijnKleur = Kleur.Rood;
Console.WriteLine(MijnKleur);
> ```

---

## Opdracht 2
Verander de waarde van een enum en print deze opnieuw.

### Specificaties
- Neem de code over van opdracht 1.
- De waarde van `MijnKleur` wordt aangepast naar een andere waarde in de `enum` (bijvoorbeeld `Kleur.Blauw`).
- Print de nieuwe waarde van `MijnKleur`.

### Verwachte output
Als de nieuwe waarde van `MijnKleur == Kleur.Blauw`, komt in de console:
```
Blauw
```

### Nu jij
``` csharp runner
using System;

enum Kleur {}


Console.WriteLine(MijnKleur); // Print Rood


Console.WriteLine(MijnKleur); // Print Blauw
``` 

> [!info]- Mogelijke uitwerking
> ``` csharp
> using System;
> 
> enum Kleur 
> {
> 	Rood,
>     Groen,
>     Blauw
> }
> 
> Kleur MijnKleur = Kleur.Rood;
> Console.WriteLine(MijnKleur);
> 
> MijnKleur = Kleur.Blauw;
> Console.WriteLine(MijnKleur);
> ```

---

## Opdracht 3
Gebruik een `enum` om foutcodes te definiëren en verwerk deze in een `switch`-statement.

Indien je nog niet bekend bent met een switch-statement, dan kan je eerst kijken naar de [[1. Uitleg Switch|switch]].

### Specificaties
- Maak een `enum` `FoutCode` met de waarden `GeenFout`, `Waarschuwing`, en `Fout`.
- Gebruik een `switch`-statement om een passende foutmelding te tonen voor elke foutcode.

### Verwachte output
Als `code = FoutCode.Waarschuwing`, komt in de console:

```
Dit is een waarschuwing.
```

### Nu jij
```csharp
using System;

enum FoutCode {}

FoutCode code = ;

switch (code)
{
}
```

> [!info]- Mogelijke uitwerking
> 
> ```csharp
> using System;
> 
> enum FoutCode
> {
>     GeenFout,
>     Waarschuwing,
>     Fout
> }
> 
> FoutCode code = FoutCode.Waarschuwing;
> 
> switch (code)
> {
>     case FoutCode.GeenFout:
>         Console.WriteLine("Geen fout gevonden.");
>         break;
>     case FoutCode.Waarschuwing:
>         Console.WriteLine("Dit is een waarschuwing.");
>         break;
>     case FoutCode.Fout:
>         Console.WriteLine("Er is een fout opgetreden.");
>         break;
> }
> ```

---

## Opdracht 4
Maak een `enum` met waarden en gebruik een loop om alle waarden te printen.

### Specificaties
- Maak een `enum` `Dag` met de dagen van de week (`Maandag`, `Dinsdag`, `Woensdag`, etc.).
- Gebruik een `foreach`-loop om elke waarde van de `enum` te printen.

### Verwachte output
```
Maandag  
Dinsdag  
Woensdag  
Donderdag  
Vrijdag  
Zaterdag  
Zondag  
```

### Nu jij
```csharp
using System;


foreach (Dag dag in )
{
    Console.WriteLine(dag);
}
```

> [!info]- Mogelijke uitwerking
> 
> ```csharp
> using System;
> 
> enum Dag
> {
>     Maandag,
>     Dinsdag,
>     Woensdag,
>     Donderdag,
>     Vrijdag,
>     Zaterdag,
>     Zondag
> }
> 
> foreach (Dag dag in Enum.GetValues(typeof(Dag)))
> {
>     Console.WriteLine(dag);
> }
> ```

---

## Opdracht 5

Gebruik een `enum` met bijbehorende numerieke waarden en bereken het verschil.

### Specificaties
- Maak een `enum` `Maand` waarin de maanden `Januari = 1`, `Februari = 2`,  `Maart = 3` tot en met `December = 12`.
- Bereken het verschil in maanden tussen twee waarden, bijvoorbeeld `Maand.Januari` en `Maand.Maart`.

### Verwachte output
Als `startMaand = Oktober` & `eindMaand = December`, komt in de console:
```
Het verschil is 2 maanden.
```

### Nu jij
```csharp
using System;


Maand startMaand = ;
Maand eindMaand = ;


```

> [!info]- Mogelijke uitwerking
> 
> ```csharp
> using System;
> 
> enum Maand
> {
>     Januari = 1,
>     Februari = 2,
>     Maart = 3,
>     April = 4,
>     Mei = 5,
>     Juni = 6,
>     Juli = 7,
>     Augustus = 8,
>     September = 9,
>     Oktober = 10,
>     November = 11,
>     December = 12
> }
> 
> Maand startMaand = Maand.Januari;
> Maand eindMaand = Maand.Maart;
> 
> int verschil = (int)eindMaand - (int)startMaand;
> Console.WriteLine($"Het verschil is {verschil} maanden.");
> ```

---

## Opdracht 6
Maak een enum voor dieren en print een specifiek dier.

### Specificaties
- Maak een `enum` `Dier` met de waarden `Hond`, `Kat`, `Vogel`.
- Print de waarde `Kat`.

### Verwachte output
```
Kat
```

### Nu jij
```csharp
using System;


```

> [!info]- Mogelijke uitwerking
> 
> ```csharp
> using System;
> 
> enum Dier
> {
>     Hond,
>     Kat,
>     Vogel
> }
> 
> Dier mijnDier = Dier.Kat;
> Console.WriteLine(mijnDier);
> ```

---

## Opdracht 7
Controleer of een bepaald voertuig een auto is met een `if`-statement.

### Specificaties
- Maak een `enum` `Voertuig` met de waarden `Auto`, `Fiets`, `Bus`.
- Controleer met een `if`-statement of de variabele `mijnVoertuig` gelijk is aan `Voertuig.Auto`.
- Print `"Dit is een auto."` als het waar is.

### Verwachte output
Als `mijnVoertuig = Voertuig.Auto`, komt in de console:
```
Dit is een auto.
```

### Nu jij
```csharp
using System;


```

> [!info]- Mogelijke uitwerking
> 
> ```csharp
> using System;
> 
> enum Voertuig
> {
>     Auto,
>     Fiets,
>     Bus
> }
> 
> Voertuig mijnVoertuig = Voertuig.Auto;
> 
> if (mijnVoertuig == Voertuig.Auto)
> {
>     Console.WriteLine("Dit is een auto.");
> }
> ```

---

## Opdracht 8
Vergelijk twee enum-waarden van maaltijden en print of ze hetzelfde zijn.

### Specificaties
- Maak een `enum` `Maaltijd` met de waarden `Ontbijt`, `Lunch`, `Diner`.
- Maak twee variabelen: `Maaltijd eersteMaaltijd` en `Maaltijd tweedeMaaltijd`.
- Vergelijk ze en print `"De maaltijden zijn hetzelfde."` of `"De maaltijden zijn verschillend."`.

### Verwachte output
Als `eersteMaaltijd = Maaltijd.Ontbijt` & `tweedeMaaltijd = Maaltijd.Lunch`, komt in de console:
```
De maaltijden zijn verschillend.
```

### Nu jij
```csharp
using System;

enum Maaltijd {}

Maaltijd eersteMaaltijd = ;
Maaltijd tweedeMaaltijd = ;

if () {
    Console.WriteLine("De maaltijden zijn hetzelfde.");
} else {
    Console.WriteLine("De maaltijden zijn verschillend.");
}
```

> [!info]- Mogelijke uitwerking
> 
> ```csharp
> using System;
> 
> enum Maaltijd
> {
>     Ontbijt,
>     Lunch,
>     Diner
> }
> 
> Maaltijd eersteMaaltijd = Maaltijd.Ontbijt;
> Maaltijd tweedeMaaltijd = Maaltijd.Lunch;
> 
> if (eersteMaaltijd == tweedeMaaltijd)
> {
>     Console.WriteLine("De maaltijden zijn hetzelfde.");
> }
> else
> {
>     Console.WriteLine("De maaltijden zijn verschillend.");
> }
> ```
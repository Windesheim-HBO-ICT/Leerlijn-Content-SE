---
title: 1. Uitleg Stack
taxonomie:
  - ib-19.2.Stack.OI
---

## Wat is een Stack?
Een **stack** (Nederlands: **stapel**) is een container waarvan alleen
het laatst toegevoegde element beschikbaar is. Nieuwe elementen worden
bovenop toegevoegd. Dit principe wordt ook wel **LIFO** (Last In First
Out) genoemd.

## Hoe zit een stack in elkaar?

Een **stack** heeft de volgende bewerkingen:
- Push : het toevoegen van een element bovenop de **stack**
- Pop : het afnemen van het bovenste element van de **stack**
- Count : het aantal elementen in de **stack**
- Peek : het bekijken van het bovenste element, zonder het eraf te halen
- Clear : het leegmaken van de **stack**

![[1.1 stack_300h.png]]
*Figuur 1: Een stack*

![[1.2 stack werking 500px.png]]
*Figuur 1: Een stack*


```csharp
int[] scores = new int[3];   // Array met 3 plekken

scores[0] = 10;
scores[1] = 20;
scores[2] = 30;

Console.WriteLine(scores[1]);  // Output: 20
```

De initialisatie kan ook korter:
```csharp
int[] scores = { 10, 20, 30, 40 };

Console.WriteLine(scores[2]);  // Output: 30
Console.WriteLine(scores.Length);  // Output: 4
```

## Hoe gebruik je [onderwerp]?

> [!TIP] Casus
>Je wil een gebruiker 5 getallen laten invoeren en hier het gemiddelde van berekenen.

**Mogelijke uitwerking van de casus**
```csharp
// 1. Een array aanmaken voor 5 getallen
int[] getallen = new int[5];
int totaal = 0;

// 2. Input van de gebruiker vragen
for (int i = 0; i < getallen.Length; i++)
{
    Console.Write("Voer getal " + (i + 1) + " in: ");
    string invoer = Console.ReadLine();
    getallen[i] = int.Parse(invoer);  // omzetten naar een getal
    totaal += getallen[i];            // optellen bij totaal
}

// 3. Gemiddelde berekenen
double gemiddelde = (double)totaal / getallen.Length;

// 4. Resultaat tonen
Console.WriteLine("Je invoer:");
foreach (int getal in getallen)
{
    Console.WriteLine("- " + getal);
}

Console.WriteLine("Gemiddelde: " + gemiddelde);
```

---

> Volgende stap: [verwijzing naar SPA/PI]
